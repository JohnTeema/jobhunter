roles:
  - product-manager
  - backend-engineer
keywords_priority:
  - python
  - fastapi
  - django
  - product-led
  - a/b testing
  - data science
  - ml
  - api design
  - postgresql
  - saas
locations:
  - remote
  - nigeria
  - united-states
salary_min_usd: 150000
score_threshold: 5
max_results: 20
bio: >
  Technical Product Manager and backend engineer with data science / ML skills.
  Product strategy + hands-on Python/FastAPI backend + ML model ops.
  Nigerian-based, fully remote or Nigeria/US timezone.
  Agile delivery, API design, SQL, A/B testing, stakeholder comms.
sources:
  remoteok:    { enabled: true,  pages: 1 }   # RSS feed — reliable
  remotive:    { enabled: true,  pages: 1 }   # public API — reliable
  duckduckgo:  { enabled: true,  pages: 1 }   # site: queries — reliable
  indeed:      { enabled: false, pages: 1 }   # 403 — needs Playwright fallback
  jobberman:   { enabled: false, pages: 1 }   # Cloudflare — needs Playwright fallback
  linkedin:    { enabled: false, pages: 1 }   # 403 — needs Playwright fallback
  wellfound:   { enabled: false, pages: 1 }   # 403 — needs Playwright fallback
  wwr:         { enabled: false, pages: 1 }   # 403 — needs Playwright fallback
  naukri:      { enabled: false, pages: 1 }   # 0 results — selector fix needed
